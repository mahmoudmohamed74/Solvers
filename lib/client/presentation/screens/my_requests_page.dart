import 'package:flutter/material.dart';

class ClientMyRequestPage extends StatelessWidget {
  const ClientMyRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("ClientMyRequestPage"));
  }
}
